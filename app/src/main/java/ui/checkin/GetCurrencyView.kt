package ui.checkin

import data.remote.response.CurrencyResponse

data class GetCurrencyView (val currencyResponse: CurrencyResponse
//... other data fields that may be accessible to the UI)
)
